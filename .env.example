# REQUIRED: Dashboard password
VAULT_PASSWORD=changeme

# REQUIRED: Public URL where the hub is accessible (used for enrollment links)
# Must NOT have a trailing slash
# Example: https://vault.example.com
VAULT_EXTERNAL_URL=

# Optional: Host port mapping (default: 8080)
VAULT_PORT=8080

# Optional: Directory containing pre-built ssh-vault binaries for enrollment downloads
# Binaries should be named ssh-vault_{os}_{arch} (e.g., ssh-vault_linux_amd64)
# Example: /opt/vault/dist
VAULT_DIST_DIR=
